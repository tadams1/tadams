package com.tadams.chess;

public class BoardScanner {

	
	public pieceMove scanBoard(int[] b, Boolean colour) {
		
		for(int i=0; i < b.length; i++) {
			if(b[i]!=-1) {
				
			}
			
		}
		// Need to get colour
		// Need to get pieces
		// Need to get possible moves
		// Need to call recursive
		
	}
	
	
	public class pieceMove {
		
	}
}
