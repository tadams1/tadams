<!DOCTYPE html>
<html>

  <script type="text/javascript" src="https://maps.googleapis.com/maps/api/js?key=AIzaSyAQ6LMGS6ebrYR_ABSnMfudwc7sgwgoTWg"></script>
    <script src="//cdnjs.cloudflare.com/ajax/libs/jquery/2.1.3/jquery.min.js"></script>
    <link rel="stylesheet" href="//code.jquery.com/ui/1.11.4/themes/smoothness/jquery-ui.css">
    <script src="//code.jquery.com/ui/1.11.4/jquery-ui.js"></script>
    <script type="text/javascript">

      var theMap;
      var thePoints;
      var theMarker;

      var pageInfo;
      var formattedAddr;
      var infowindow;

      function addAccordion() {
        
      }

      function initialize() {
          
        var mapOptions = {
          zoom: 4,
          center: {lat: -28, lng: 137.883},
              mapTypeId: google.maps.MapTypeId.TERRAIN
          };

        theMap = new google.maps.Map(document.getElementById('map'), mapOptions);
        theMap.data.setStyle(function(feature) {
          return {icon:feature.getProperty('icon')};
        });
        thePoints = <%- points %>;
        pageInfo = <%- pageInfo %>;
        var infowindow = new google.maps.InfoWindow();

        theMap.data.addListener('click', function(event) {
          if(event.feature == null) {
            infowindow.close();
            return;
          }  
          var myHTML = event.feature.getProperty("type") + "<p>" + "Type: " +  event.feature.getProperty("typetext") + "<p>" + "Comment: " +  event.feature.getProperty("comment");

          infowindow.setContent("<div style='width:150px; text-align: center;'>" + myHTML + "</div>");
          infowindow.setPosition(event.latLng);
          infowindow.setOptions({pixelOffset: new google.maps.Size(0,-30)});
          infowindow.open(theMap);
        });

        buildPage('threat', pageInfo.types);
        buildPage('action', pageInfo.types);
        buildPage('layer', pageInfo.types);

      }

    function buildPage(thename, thearray) {
      var tchk = $('#' + thename + 'typechk');
      var featureType;
      if(thename=="layer"){
        featureType = "Polygon";
      }else{
        featureType = "Point";
      }
      var theList = $('<ul class="' + thename + '-checkbox"></ul>');
      for(var i = 0; i < thearray.length; i++) {
          if(thearray[i].type == thename) {
            var theItem = $('<li></li>');
            theItem.append('<input type="checkbox" onclick=updateJSON(this,"' + featureType + '","' + thearray[i].id + '") + id="' + thename  + i + '" value="' + thearray[i].id + '" /><label for="' + thename + i + '">' + thearray[i].text + '</label>');
            theList.append(theItem);
          }
      }
      tchk.append(theList);      
    }


    google.maps.event.addDomListener(window, 'load', initialize);

    function updateJSON(chkbox, geotype, dbtype) {
      if(chkbox.checked) {
        theMap.data.loadGeoJson('http://10.1.1.8:3000/maps?geotype=' + geotype + '&type='+ dbtype);
      }else{
        var callback=function(feature) {
          
          if(feature.getProperty('typeid') == dbtype || feature.getProperty('letter') == dbtype) {
            theMap.data.remove(feature);
          }
        };
        theMap.data.forEach(callback);
      }
    }
</script>



<head>
	<meta charset="utf-8">
    <meta name="viewport" content="width=device-width" />
    <meta name="Description" content="BirdLife Australia Shorebird Action Map">
    <meta name="author" content="Ecocene Solutions">
    <meta name="Copyright" content="Copyright (c) 2015 Ecocene Solutions, all rights reserved.">
    <title>Shorebord Action Map | BirdLife Australia</title>
    <link rel="stylesheet" href="stylesheets/default.css" />
    <!--[if IE]>
    <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
    <![endif]-->
</head>
<body>
    <section id="mainRight" class="clear">
        <header>
            <h1><span>Help protect our shorebirds!</span></h1>
        </header>
        <nav>
            <ul>
                <li><a href="view.html" class="selected">View Records</a></li>
                <li><a href="threat.html">Log a threat</a></li>
                <li><a href="#">Log an action</a></li>
                <li><a href="#">About</a></li>
            </ul>
        </nav>
		<section class="index">
		
      <div class="centerpane" id="map">
        </section>
        <!-- end columns -->
        <footer>
		<img src="images/BirdLife-Logo.jpg" alt="" border="0">
		        <h5 id="signature">&copy; 2015 BirdLife Australia</h5>
	
            <ul>
                <li><a href="index.html">Home</a></li>
                <li><a href="services.html">Services</a></li>
                <li><a href="about.html">About</a></li>
                <li><a href="contact.html">Contact</a></li>
            </ul>
        </footer>
    </section>
    <!-- end mainRight -->
    <div id="logoTxt">
        <h1><span></span></h1>
        <h2><span></span></h2>
    </div>
</body>
</html>
